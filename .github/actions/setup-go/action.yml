name: Setup Go
description: |
  Setup Go

inputs:
  cache:
    description: Cache
    required: false
    default: "true"

runs:
  using: "composite"
  steps:
    - id: go-setup
      uses: actions/setup-go@v6
      with:
        go-version-file: go.mod
        cache: ${{ inputs.cache }}
